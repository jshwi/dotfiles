###############################################################################
#                            SPACESHIP CONFIG                                 #
###############################################################################
# #============================================================================
# # Options
# #----------------------------------------------------------------------------
# # You have ability to customize or disable specific elements of Spaceship.
# # All options must be overridden in your .zshrc file after the theme.
# #
# # Colors for sections can be basic colors or color codes.
# #
# # Note: the symbol · in this document represents a regular space character,
# # it is used to clearly indicate when an option default value starts or ends
# # with a space.
# #
# # Order
# # You can specify the order of prompt section using SPACESHIP_PROMPT_ORDER
# # option. Use Zsh array syntax to define your own prompt order.
# #
# # The order also defines which sections that Spaceship loads. If you're
# # struggling with slow prompt, you can just omit the sections that you don't
# # use, and they won't be loaded.
# #
# # The default order is:
# #============================================================================
# SPACESHIP_PROMPT_ORDER=(
#   time          # Time stamps section
#   user          # Username section
#   dir           # Current directory section
#   host          # Hostname section
#   git           # Git section (git_branch + git_status)
#   hg            # Mercurial section (hg_branch  + hg_status)
#   package       # Package version
#   node          # Node.js section
#   ruby          # Ruby section
#   elixir        # Elixir section
#   xcode         # Xcode section
#   swift         # Swift section
#   golang        # Go section
#   php           # PHP section
#   rust          # Rust section
#   haskell       # Haskell Stack section
#   julia         # Julia section
#   docker        # Docker section
#   aws           # Amazon Web Services section
#   venv          # virtualenv section
#   conda         # conda virtualenv section
#   pyenv         # Pyenv section
#   dotnet        # .NET section
#   ember         # Ember.js section
#   kubecontext   # Kubectl context section
#   terraform     # Terraform workspace section
#   exec_time     # Execution time
#   line_sep      # Line break
#   battery       # Battery level and status
#   vi_mode       # Vi-mode indicator
#   jobs          # Background jobs indicator
#   exit_code     # Exit code section
#   char          # Prompt character
# )
#
# # You can also add items to the right prompt by specifying them in the
# # SPACESHIP_RPROMPT_ORDER option. By default SPACESHIP_RPROMPT_ORDER is
# # empty.

#==============================================================================
# Glyphs / Icons
#------------------------------------------------------------------------------
NODE_GLYPH=' '

ARROW_GLYPH='  '

GIT_GLYPH=' '

PYTHON_GLYPH='🐍 '

NPM_GLYPH='📦 '
HOME_GLYPH=' '

#==============================================================================

# #============================================================================
# # Prompt
# #----------------------------------------------------------------------------
# # This group of options defines a behaviour of prompt
# # and standard parameters for sections displaying.
# #=+==========================================================================
SPACESHIP_PROMPT_ADD_NEWLINE=true
# SPACESHIP_PROMPT_SEPARATE_LINE=true
# SPACESHIP_PROMPT_FIRST_PREFIX_SHOW=false
# SPACESHIP_PROMPT_PREFIXES_SHOW=true
# SPACESHIP_PROMPT_SUFFIXES_SHOW=true
# SPACESHIP_PROMPT_DEFAULT_PREFIX=via
# SPACESHIP_PROMPT_DEFAULT_SUFFIX=false
#
# #============================================================================
# # Char
# #============================================================================
# SPACESHIP_CHAR_PREFIX=false
# SPACESHIP_CHAR_SUFFIX=false
# SPACESHIP_CHAR_SYMBOL=➜
SPACESHIP_CHAR_SYMBOL="$ARROW_GLYPH"
# SPACESHIP_CHAR_SYMBOL_ROOT=$SPACESHIP_CHAR_SYMBOL
# SPACESHIP_CHAR_SYMBOL_SECONDARY=$SPACESHIP_CHAR_SYMBOL
# SPACESHIP_CHAR_COLOR_SUCCESS=green
# SPACESHIP_CHAR_COLOR_FAILURE=red
# SPACESHIP_CHAR_COLOR_SECONDARY=yellow
#
# #============================================================================
# # Time (time)
# #----------------------------------------------------------------------------
# # Disabled by default. Set SPACESHIP_TIME_SHOW to true in your .zshrc, if you
# # ``need to show time stamps.
# #============================================================================
SPACESHIP_TIME_SHOW=true
# SPACESHIP_TIME_PREFIX=at·
# SPACESHIP_TIME_SUFFIX=$SPACESHIP_PROMPT_DEFAULT_SUFFIX
SPACESHIP_TIME_COLOR=magenta
# SPACESHIP_TIME_FORMAT=false
# SPACESHIP_TIME_12HR=false
#
# #============================================================================
# # Username (user)
# #----------------------------------------------------------------------------
# # By default, a username is shown only when it's not the same as $LOGNAME,
# # when you're connected via SSH or when you're root. Root user is highlighted
# # in SPACESHIP_USER_COLOR_ROOT color (red as default).
# #============================================================================
# SPACESHIP_USER_SHOW=true
# SPACESHIP_USER_PREFIX=with·
# SPACESHIP_USER_SUFFIX=$SPACESHIP_PROMPT_DEFAULT_SUFFIX
# SPACESHIP_USER_COLOR=yellow
# SPACESHIP_USER_COLOR_ROOT=red
# SPACESHIP_USER_SHOW=true
#
# #============================================================================
# # Hostname (host)
# #----------------------------------------------------------------------------
# # Hostname is shown only when you're connected via SSH unless you change this
# # behavior.
# #============================================================================
# SPACESHIP_HOST_SHOW=true
# SPACESHIP_HOST_SHOW_FULL=false
# SPACESHIP_HOST_PREFIX=at·
# SPACESHIP_HOST_SUFFIX=$SPACESHIP_PROMPT_DEFAULT_SUFFIX
# SPACESHIP_HOST_COLOR=blue
# SPACESHIP_HOST_COLOR_SSH=green
#
# #============================================================================
# # Directory (dir)
# #----------------------------------------------------------------------------
# # Directory is always shown and truncated to the value of
# # SPACESHIP_DIR_TRUNC. While you are in repository, it shows only root
# # directory and folders inside it. If current directory is write-protected or
# # if current user has not enough rights to write in it, a padlock
# # (by default) is displayed as a suffix.
# #============================================================================
# SPACESHIP_DIR_SHOW=true
SPACESHIP_DIR_PREFIX="in "
#SPACESHIP_DIR_SUFFIX=$SPACESHIP_PROMPT_DEFAULT_SUFFIX
SPACESHIP_DIR_TRUNC=0
# SPACESHIP_DIR_TRUNC_PREFIX=false
# SPACESHIP_DIR_TRUNC_REPO=true
# SPACESHIP_DIR_COLOR=cyan
# SPACESHIP_DIR_LOCK_SYMBOL=·
# SPACESHIP_DIR_LOCK_COLOR=red
#
# #==============================================================================
# # Git (git)
# #------------------------------------------------------------------------------
# # Git section is consists with git_branch and git_status subsections. It is
# # shown only in Git repositories.
# #==============================================================================
# SPACESHIP_GIT_SHOW=true
# SPACESHIP_GIT_PREFIX=on·
# SPACESHIP_GIT_SUFFIX=$SPACESHIP_PROMPT_DEFAULT_SUFFIX
# SPACESHIP_GIT_SYMBOL=·
SPACESHIP_GIT_SYMBOL="$GIT_GLYPH"
#
# #==============================================================================
# # Git branch (git_branch)
# #==============================================================================
# SPACESHIP_GIT_BRANCH_SHOW=true
# SPACESHIP_GIT_BRANCH_PREFIX=$SPACESHIP_GIT_SYMBOL
# SPACESHIP_GIT_BRANCH_SUFFIX=false
# SPACESHIP_GIT_BRANCH_COLOR=false
#
# #==============================================================================
# # Git status (git_status)
# #------------------------------------------------------------------------------
# # Git status indicators is shown only when you have dirty repository.
# #==============================================================================
# SPACESHIP_GIT_STATUS_SHOW=true
# SPACESHIP_GIT_STATUS_PREFIX='·['
# SPACESHIP_GIT_STATUS_SUFFIX=']'
# SPACESHIP_GIT_STATUS_COLOR=red
# SPACESHIP_GIT_STATUS_UNTRACKED=?
# SPACESHIP_GIT_STATUS_ADDED=+
# SPACESHIP_GIT_STATUS_MODIFIED=!
# SPACESHIP_GIT_STATUS_RENAMED=»
# SPACESHIP_GIT_STATUS_DELETED=✘
# SPACESHIP_GIT_STATUS_STASHED=$
# SPACESHIP_GIT_STATUS_UNMERGED==
# SPACESHIP_GIT_STATUS_AHEAD=⇡
# SPACESHIP_GIT_STATUS_BEHIND=⇣
# SPACESHIP_GIT_STATUS_DIVERGED=⇕
#
# #==============================================================================
# # Mercurial (hg)
# #------------------------------------------------------------------------------
# # Mercurial section is consists with hg_branch and hg_status subsections. It is
# # shown only in Mercurial repositories.
# #==============================================================================
# SPACESHIP_HG_SHOW=true
# SPACESHIP_HG_PREFIX=on·
# SPACESHIP_HG_SUFFIX=$SPACESHIP_PROMPT_DEFAULT_SUFFIX
# SPACESHIP_HG_SYMBOL=☿·
#
# #==============================================================================
# # Mercurial branch (hg_branch)
# #------------------------------------------------------------------------------
# # Shows Mercurial bookmarks when available, otherwise shows Mercurial branch
# # information.
# #==============================================================================
# SPACESHIP_HG_BRANCH_SHOW=true
# SPACESHIP_HG_BRANCH_PREFIX=$SPACESHIP_HG_SYMBOL
# SPACESHIP_HG_BRANCH_SUFFIX=$SPACESHIP_PROMPT_DEFAULT_SUFFIX
# SPACESHIP_HG_BRANCH_COLOR=magenta
#
# #==============================================================================
# # Mercurial status (hg_status)
# #------------------------------------------------------------------------------
# # Mercurial status indicators is shown only when you have dirty repository.
# #==============================================================================
# SPACESHIP_HG_STATUS_SHOW=true
# SPACESHIP_HG_STATUS_PREFIX='['
# SPACESHIP_HG_STATUS_SUFFIX=']'
# SPACESHIP_HG_STATUS_COLOR=red
# SPACESHIP_HG_STATUS_UNTRACKED=?
# SPACESHIP_HG_STATUS_ADDED=+
# SPACESHIP_HG_STATUS_MODIFIED=!
# SPACESHIP_HG_STATUS_DELETED=✘
#
# #==============================================================================
# # Package version (package)
# #------------------------------------------------------------------------------
# # Works for npm and cargo at the moment. Please, help us improve this section!
# # npm — npm package contains a package.json file. We use jq, python to parse
# # package version for improving performance and node as a fallback. Install jq
# # for improved performance of this section (Why?)
# # cargo — cargo package contains a Cargo.toml file. Currently, we use cargo
# # pkgid, it depends on Cargo.lock. So if package version isn't shown, you
# # mayneed to run some command like cargo build which can generate Cargo.lock
# # file.
# # Note: This is the version of the package you are working on, not the version
# # of package manager itself.
# #==============================================================================
# SPACESHIP_PACKAGE_SHOW=true
# SPACESHIP_PACKAGE_PREFIX=is
# SPACESHIP_PACKAGE_SUFFIX=$SPACESHIP_PROMPT_DEFAULT_SUFFIX
SPACESHIP_PACKAGE_SYMBOL="$NPM_GLYPH"
# SPACESHIP_PACKAGE_COLOR=red
#
# #==============================================================================
# # Node.js (node)
# #------------------------------------------------------------------------------
# # Node.js section is shown only in directories that contain package.json file,
# # or node_modules folder, or any other file with .js extension.
# #
# # If you set SPACESHIP_NODE_DEFAULT_VERSION to the default Node.js version and
# # your current version is the same as SPACESHIP_NODE_DEFAULT_VERSION, then
# # Node.js section will be hidden.
# #==============================================================================
# SPACESHIP_NODE_SHOW=true
# SPACESHIP_NODE_PREFIX=$SPACESHIP_PROMPT_DEFAULT_PREFIX
# SPACESHIP_NODE_SUFFIX=$SPACESHIP_PROMPT_DEFAULT_SUFFIX
# SPACESHIP_NODE_SYMBOL=⬢·
SPACESHIP_NODE_SYMBOL="$NODE_GLYPH"
# SPACESHIP_NODE_DEFAULT_VERSION=false
# SPACESHIP_NODE_COLOR=green
#
# #==============================================================================
# # Ruby (ruby)
# #------------------------------------------------------------------------------
# # Ruby section is shown only in directories that contain Gemfile, or Rakefile,
# # or any other file with .rb extension.
# #==============================================================================
# SPACESHIP_RUBY_SHOW=true
# SPACESHIP_RUBY_PREFIX=$SPACESHIP_PROMPT_DEFAULT_PREFIX
# SPACESHIP_RUBY_SUFFIX=$SPACESHIP_PROMPT_DEFAULT_SUFFIX
# SPACESHIP_RUBY_SYMBOL=💎·
# SPACESHIP_RUBY_COLOR=red
#
# #==============================================================================
# # Elm (elm)
# #------------------------------------------------------------------------------
# # Elm section is shown only in directories that contain elm.json file,
# # elm-package.json file, elm-stuff directory, or any other file with .elm
# # extension.
# #==============================================================================
# SPACESHIP_ELM_SHOW=true
# SPACESHIP_ELM_PREFIX=$SPACESHIP_PROMPT_DEFAULT_PREFIX
# SPACESHIP_ELM_SUFFIX=$SPACESHIP_PROMPT_DEFAULT_SUFFIX
# SPACESHIP_ELM_SYMBOL=🌳·
# SPACESHIP_ELM_COLOR=cyan
#
# #==============================================================================
# # Elixir (elixir)
# #------------------------------------------------------------------------------
# # Elixir section is shown only in directories that contain mix.exs, or any other
# # file with .ex or .exs extension. If the current elixir version is the same as
# # the version set in SPACESHIP_ELIXIR_DEFAULT_VERSION, the elixir section will
# # be hidden.
# #==============================================================================
# SPACESHIP_ELIXIR_SHOW=true
# SPACESHIP_ELIXIR_PREFIX=$SPACESHIP_PROMPT_DEFAULT_PREFIX
# SPACESHIP_ELIXIR_SUFFIX=$SPACESHIP_PROMPT_DEFAULT_SUFFIX
# SPACESHIP_ELIXIR_DEFAULT_VERSION=false
# SPACESHIP_ELIXIR_SYMBOL=💧·
# SPACESHIP_ELIXIR_COLOR=magenta
#
# #==============================================================================
# # Xcode (xcode)
# #------------------------------------------------------------------------------
# # Shows current version of Xcode. Local version has more priority than global.
# #==============================================================================
# SPACESHIP_XCODE_SHOW_LOCAL=true
# SPACESHIP_XCODE_SHOW_GLOBAL=true
# SPACESHIP_XCODE_PREFIX=$SPACESHIP_PROMPT_DEFAULT_PREFIX
# SPACESHIP_XCODE_SUFFIX=$SPACESHIP_PROMPT_DEFAULT_SUFFIX
# SPACESHIP_XCODE_SYMBOL=🛠·
# SPACESHIP_XCODE_COLOR=blue
#
# #==============================================================================
# # Swift (swift)
# #------------------------------------------------------------------------------
# # Shows current version of Swift. Local version has more priority than global.
# #==============================================================================
# SPACESHIP_SWIFT_SHOW_LOCAL=true
# SPACESHIP_SWIFT_SHOW_GLOBAL=false
# SPACESHIP_SWIFT_PREFIX=$SPACESHIP_PROMPT_DEFAULT_PREFIX
# SPACESHIP_SWIFT_SUFFIX=$SPACESHIP_PROMPT_DEFAULT_SUFFIX
# SPACESHIP_SWIFT_SYMBOL=🐦·
# SPACESHIP_SWIFT_COLOR=yellow
#
# #==============================================================================
# # Go (golang)
# #------------------------------------------------------------------------------
# # Go section is shown only in directories that contain go.mod, Godeps,
# # glide.yaml, any other file with .go extension, or when current directory is
# # in the Go workspace defined in $GOPATH.
# #
# # If you are using a development version of Go, the version uses git commit
# # hash instead.
# #
# # For example:
# #
# # devel:5efe9a8f11 for development version
# # v1.11.4 for release version
# #==============================================================================
# SPACESHIP_GOLANG_SHOW=true
# SPACESHIP_GOLANG_PREFIX=$SPACESHIP_PROMPT_DEFAULT_PREFIX
# SPACESHIP_GOLANG_SYMBOL=🐹·
# SPACESHIP_GOLANG_COLOR=cyan
#
# #==============================================================================
# # PHP (php)
# #------------------------------------------------------------------------------
# # PHP section is shown only in directories that contain composer.json file, or
# # any other file with .php extension.
# #==============================================================================
# SPACESHIP_PHP_SHOW=true
# SPACESHIP_PHP_PREFIX=$SPACESHIP_PROMPT_DEFAULT_PREFIX
# SPACESHIP_PHP_SUFFIX=$SPACESHIP_PROMPT_DEFAULT_SUFFIX
# SPACESHIP_PHP_SYMBOL=🐘·
# SPACESHIP_PHP_COLOR=blue
#
# #==============================================================================
# # Rust (rust)
# #------------------------------------------------------------------------------
# # Rust section is shown only in directories that contain Cargo.toml or any
# # other file with .rs extension.
# #==============================================================================
# SPACESHIP_RUST_SHOW=true
# SPACESHIP_RUST_PREFIX=$SPACESHIP_PROMPT_DEFAULT_PREFIX
# SPACESHIP_RUST_SUFFIX=$SPACESHIP_PROMPT_DEFAULT_SUFFIX
# SPACESHIP_RUST_SYMBOL=𝗥·
# SPACESHIP_RUST_COLOR=red
# SPACESHIP_RUST_VERBOSE_VERSION=false
#
# #==============================================================================
# # Haskell (haskell)
# #------------------------------------------------------------------------------
# # Haskell section is shown only in directories that contain stack.yaml file.
# #==============================================================================
# SPACESHIP_HASKELL_SHOW=true
# SPACESHIP_HASKELL_PREFIX=$SPACESHIP_PROMPT_DEFAULT_PREFIX
# SPACESHIP_HASKELL_SUFFIX=$SPACESHIP_PROMPT_DEFAULT_SUFFIX
# SPACESHIP_HASKELL_SYMBOL=λ·
# SPACESHIP_HASKELL_COLOR=red
#
# #==============================================================================
# # Julia (julia)
# #------------------------------------------------------------------------------
# # Julia section is shown only in directories that contain file with .jl
# # extension.
# #==============================================================================
# SPACESHIP_JULIA_SHOW=true
# SPACESHIP_JULIA_PREFIX=$SPACESHIP_PROMPT_DEFAULT_PREFIX
# SPACESHIP_JULIA_SUFFIX=$SPACESHIP_PROMPT_DEFAULT_SUFFIX
# SPACESHIP_JULIA_SYMBOL=ஃ·
# SPACESHIP_JULIA_COLOR=green
#
# #==============================================================================
# # Docker (docker)
# #------------------------------------------------------------------------------
# # Docker section is shown only in directories that contain Dockerfile or it's
# # possible to run docker-compose.
# #
# # docker-compose will run only if there is docker-compose.yml, or other file(s)
# # specified with COMPOSE_FILE are accessible.
# #
# # The environment variable COMPOSE_PATH_SEPARATOR is supported too. For more
# # information see Compose CLI environment variables.
# #==============================================================================
# SPACESHIP_DOCKER_SHOW=true
# SPACESHIP_DOCKER_PREFIX=on
# SPACESHIP_DOCKER_SUFFIX=$SPACESHIP_PROMPT_DEFAULT_SUFFIX
# SPACESHIP_DOCKER_SYMBOL=🐳·
# SPACESHIP_DOCKER_COLOR=cyan
# SPACESHIP_DOCKER_VERBOSE=false
#
# #==============================================================================
# # Amazon Web Services (AWS) (aws)
# #------------------------------------------------------------------------------
# # Shows selected Amazon Web Services profile configured using AWS_PROFILE
# # variable.
# #==============================================================================
# SPACESHIP_AWS_SHOW=true
# SPACESHIP_AWS_PREFIX=using·
# SPACESHIP_AWS_SUFFIX=$SPACESHIP_PROMPT_DEFAULT_SUFFIX
# SPACESHIP_AWS_SYMBOL=☁️·
# SPACESHIP_AWS_COLOR=208
#
# #==============================================================================
# # Virtualenv (venv)
# #==============================================================================
# SPACESHIP_VENV_SHOW=true
# SPACESHIP_VENV_PREFIX=$SPACESHIP_PROMPT_DEFAULT_PREFIX
# SPACESHIP_VENV_SUFFIX=$SPACESHIP_PROMPT_DEFAULT_SUFFIX
# SPACESHIP_VENV_SYMBOL=·
SPACESHIP_VENV_SYMBOL="$PYTHON_GLYPH"
# SPACESHIP_VENV_GENERIC_NAMES=(virtualenv venv .venv)
# SPACESHIP_VENV_COLOR=blue
#
# #==============================================================================
# # Conda virtualenv (conda)
# #------------------------------------------------------------------------------
# # Show activated conda virtual environment. Disable native conda prompt by
# # conda config --set changeps1 False.
# #==============================================================================
# SPACESHIP_CONDA_SHOW=true
# SPACESHIP_CONDA_PREFIX=$SPACESHIP_PROMPT_DEFAULT_PREFIX
# SPACESHIP_CONDA_SUFFIX=$SPACESHIP_PROMPT_DEFAULT_SUFFIX
# SPACESHIP_CONDA_SYMBOL=🅒·
# SPACESHIP_CONDA_COLOR=blue
# SPACESHIP_CONDA_VERBOSE=true
#
# #==============================================================================
# # Pyenv (pyenv)
# #------------------------------------------------------------------------------
# # pyenv section is shown only in directories that contain requirements.txt,
# # pyproject.toml or any other file with .py extension.
# #==============================================================================
# SPACESHIP_PYENV_SHOW=true
# SPACESHIP_PYENV_PREFIX=$SPACESHIP_PROMPT_DEFAULT_PREFIX
# SPACESHIP_PYENV_SUFFIX=$SPACESHIP_PROMPT_DEFAULT_SUFFIX
# SPACESHIP_PYENV_SYMBOL=🐍·
# SPACESHIP_PYENV_COLOR=yellow
#
# #==============================================================================
# # .NET (dotnet)
# #------------------------------------------------------------------------------
# # .NET section is shown only in directories that contains a project.json or
# # global.json file, or a file with one of these extensions: .csproj, .xproj or
# # .sln.
# #==============================================================================
# SPACESHIP_DOTNET_SHOW=true
# SPACESHIP_DOTNET_PREFIX=$SPACESHIP_PROMPT_DEFAULT_PREFIX
# SPACESHIP_DOTNET_SUFFIX=$SPACESHIP_PROMPT_DEFAULT_SUFFIX
# SPACESHIP_DOTNET_SYMBOL=NET·
# SPACESHIP_DOTNET_COLOR=128
#
# #==============================================================================
# # Ember.js (ember)
# #------------------------------------------------------------------------------
# # Ember.js section is shown only in directories that contain a
# # ember-cli-build.js file.
# #==============================================================================
# SPACESHIP_EMBER_SHOW=true
# SPACESHIP_EMBER_PREFIX=$SPACESHIP_PROMPT_DEFAULT_PREFIX
# SPACESHIP_EMBER_SUFFIX=$SPACESHIP_PROMPT_DEFAULT_SUFFIX
# SPACESHIP_EMBER_SYMBOL=🐹·
# SPACESHIP_EMBER_COLOR=210
#
# #==============================================================================
# # Kubectl context (kubecontext)
# #------------------------------------------------------------------------------
# # Shows the active kubectl context, which consists of a cluster name and, when
# # working in a non-default namespace, also a namespace name.
# #
# # bulb Tip: If your cluster name (and thus context name) is too long, you can
# # give it a shorter name using kubectl config rename-context
# # very_long_context_name name.
# #==============================================================================
# SPACESHIP_KUBECONTEXT_SHOW=true
# SPACESHIP_KUBECONTEXT_PREFIX=at·
# SPACESHIP_KUBECONTEXT_SUFFIX=$SPACESHIP_PROMPT_DEFAULT_SUFFIX
# SPACESHIP_KUBECONTEXT_SYMBOL=☸️·
# SPACESHIP_KUBECONTEXT_COLOR=cyan
# SPACESHIP_KUBECONTEXT_NAMESPACE_SHOW=true
# SPACESHIP_KUBECONTEXT_COLOR_GROUPS=false
#
# #==============================================================================
# # Color Groups
# #------------------------------------------------------------------------------
# # To set the section to a different color based on context or namespace, you
# # can define an array of pair values in which the first value of a pair is a
# # color name to use and the second value is a regular expression pattern to
# # match against the section text (context name and/or namespace). The first
# # matched pattern will determine the color, so list order can be used to
# # prioritize patterns.
# #
# # For example, add the following to your .zshrc file:
# #==============================================================================
# SPACESHIP_KUBECONTEXT_COLOR_GROUPS=(
#   # red if namespace is "kube-system"
#   red    '\(kube-system)$'
#   # else, green if "dev-01" is anywhere in the context or namespace
#   green  dev-01
#   # else, red if context name ends with ".k8s.local" _and_ namespace is "system"
#   red    '\.k8s\.local \(system)$'
#   # else, yellow if the entire content is "test-" followed by digits, and no namespace is displayed
#   yellow '^test-[0-9]+$'
# )
#
# #==============================================================================
# # Terraform workspace (terraform)
# #------------------------------------------------------------------------------
# # Shows the active Terraform wokspace in directories that contain
# # .terraform/environment file.
# #==============================================================================
# SPACESHIP_TERRAFORM_SHOW=true
# SPACESHIP_TERRAFORM_PREFIX=$SPACESHIP_PROMPT_DEFAULT_PREFIX
# SPACESHIP_TERRAFORM_SUFFIX=$SPACESHIP_PROMPT_DEFAULT_SUFFIX
# SPACESHIP_TERRAFORM_SYMBOL=🛠️·
# SPACESHIP_TERRAFORM_COLOR=105
#
# #==============================================================================
# # Execution time (exec_time)
# #------------------------------------------------------------------------------
# # Execution time of the last command. Will be displayed if it exceeds the set
# # threshold of time.
# #==============================================================================
# SPACESHIP_EXEC_TIME_SHOW=true
# SPACESHIP_EXEC_TIME_PREFIX=took·
# SPACESHIP_EXEC_TIME_SUFFIX=$SPACESHIP_PROMPT_DEFAULT_SUFFIX
# SPACESHIP_EXEC_TIME_COLOR=yellow
# SPACESHIP_EXEC_TIME_ELAPSED=2
#
# #==============================================================================
# # Battery (battery)
# #------------------------------------------------------------------------------
# # By default, Battery section is shown only if battery level is below
# # SPACESHIP_BATTERY_THRESHOLD (default: 10%).
# #==============================================================================
# SPACESHIP_BATTERY_SHOW=true
# SPACESHIP_BATTERY_PREFIX=' '
# SPACESHIP_BATTERY_SUFFIX=$SPACESHIP_PROMPT_DEFAULT_SUFFIX
# SPACESHIP_BATTERY_SYMBOL_CHARGING=⇡
# SPACESHIP_BATTERY_SYMBOL_DISCHARGING=⇣
# SPACESHIP_BATTERY_SYMBOL_FULL=•
# SPACESHIP_BATTERY_THRESHOLD=10
# SPACESHIP_BATTERY_SHOW=true
#
# #==============================================================================
# # Vi-mode (vi_mode)
# #------------------------------------------------------------------------------
# # This section shows mode indicator only when Vi-mode is enabled.
# #==============================================================================
# SPACESHIP_VI_MODE_SHOW=true
# SPACESHIP_VI_MODE_PREFIX=' '
# SPACESHIP_VI_MODE_SUFFIX=$SPACESHIP_PROMPT_DEFAULT_SUFFIX
# SPACESHIP_VI_MODE_INSERT=[I]
# SPACESHIP_VI_MODE_NORMAL=[N]
# SPACESHIP_VI_MODE_COLOR=white
# #------------------------------------------------------------------------------
# # You can temporarily enable or disable vi-mode with handy functions (just
# # execute them in terminal as any other regular command):
# # spaceship_vi_mode_enable	Enable vi-mode for current terminal session
# # spaceship_vi_mode_disable	Disable vi-mode for current terminal session
# # Note: For oh-my-zsh users with vi-mode plugin enabled: Add export
# # RPS1="%{$reset_color%}" before source $ZSH/oh-my-zsh.sh in .zshrc to disable
# # default <<< NORMAL mode indicator in right prompt.
#
# #==============================================================================
# # Jobs (jobs)
# #------------------------------------------------------------------------------
# # This section show only when there are active jobs in the background.
# #==============================================================================
# SPACESHIP_JOBS_SHOW=true
# SPACESHIP_JOBS_PREFIX=' '
# SPACESHIP_JOBS_SUFFIX=' '
# SPACESHIP_JOBS_SYMBOL=✦
# SPACESHIP_JOBS_COLOR=blue
# SPACESHIP_JOBS_AMOUNT_PREFIX=' '
# SPACESHIP_JOBS_AMOUNT_SUFFIX=' '
# SPACESHIP_JOBS_AMOUNT_THRESHOLD=' '
#
# #==============================================================================
# # Exit code (exit_code)
# #------------------------------------------------------------------------------
# # Disabled by default. Set SPACESHIP_EXIT_CODE_SHOW to true in your .zshrc, if
# # you need to show exit code of last command.
# #==============================================================================
# SPACESHIP_EXIT_CODE_SHOW=false
# SPACESHIP_EXIT_CODE_PREFIX=' '
# SPACESHIP_EXIT_CODE_SUFFIX=' '
# SPACESHIP_EXIT_CODE_SYMBOL=✘
# SPACESHIP_EXIT_CODE_COLOR=red
